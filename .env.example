# Server-only API credentials. Never prefix secrets with NEXT_PUBLIC_.
MONKEYTYPE_APE_KEY=

# Public production URL without a trailing slash, used for sitemap and robots.
NEXT_PUBLIC_SITE_URL=

# Use this only when the contact form moves to a server-side provider.
CONTACT_PROVIDER_API_KEY=
